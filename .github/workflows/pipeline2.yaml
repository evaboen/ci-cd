name: PR Pipe

on:
    pull_request: 
        types:
            - opened

jobs:
    build:
        runs-on: ubuntu-latest

        steps: 
          - name: sleep!
            run: sleep 10

          - name: finish
            run: echo "Pull request created"
